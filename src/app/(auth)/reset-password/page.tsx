import { ResetPasswordForm } from "@/features/auth/reset-password-form";

export const metadata = { title: "Redefinir senha — Autoprocs Dispatcher" };

export default function ResetPasswordPage() {
  return <ResetPasswordForm />;
}
